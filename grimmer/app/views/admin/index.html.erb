<% content_for :custom_js do %>
  <%= javascript_include_tag "admin" %>
<% end %>
<div class="container" style="margin-top: 45px">
  <strong>admin view</strong>
</div>

<div class="container">
<div class="tab">
  <button class="tablinks" onclick="openTab(event, 'dumpster')">Dumpster</button>
  <button class="tablinks" onclick="openTab(event, 'blacklist')">Blacklist</button>
  <button class="tablinks" onclick="openTab(event, 'user_managment')">User Managment</button>
</div>

<div id="dumpster" class="tabcontent">
<% if (@dumpster_posts.length == 0)%>
  <h4> No posts in the dumpster...</h4>
<% else %>
  <table class="table table-striped table-bordered" id="dumpster_table">
      <thead>
        <strong> Dumpster managment</strong>
      <tr>
          <th style="text-align:center">Post Title</th>
          <th style="text-align:center">Author</th>
          <th style="text-align: center">Email</th>
          <th style="text-align: center"></th>
          <th></th>
      </tr>
      </thead>
      <tbody id="dumpster_table_body">
  <% @dumpster_posts.each do |post| %>
          <tr>
            <td><%= post.title %></td>
            <% owner = UserProfile.where("id= ?", post.user_id ) %>
            <% owner_user = User.where("id= ?", post.user_id) %>
            <td><%= owner.name %> / <%= owner.last_name %></td>
            <td><%= owner_user.email %></td>
            <td>
              <button class="btn btn-sm btn-primary" title="Go to post">
                <i class="fas fa-eye"></i>
              </button>
              <button class="btn btn-sm btn-primary" title="Make visible again">
                <i class="far fa-edit"></i>
              </button>
              <button class="btn btn-sm btn-danger" title="Delete post">
                <i class="far fa-trash-alt"></i>
              </button>
            </td>
          </tr>

  <% end %>
  </tbody>
  </table>
<% end %>
</div>

<div id="blacklist" class="tabcontent">
<% if (@blacklist.length == 0)%>
  <h4> No users in the blacklist...</h4>
<% else %>
  <table class="table table-striped table-bordered" id="blacklist_table">
      <thead>
        <strong> Blacklist managment</strong>
      <tr>
          <th style="text-align:center" >Id</th>
          <th style="text-align:center" >Email</th>
          <th></th>
      </tr>
      </thead>
      <tbody id="blacklist_table_body">
      <% @blacklist.each do |user| %>
        <tr>
          <td><%= user.id %></td>
          <td><%= user.email %></td>
          <td>
            <button class="btn btn-sm btn-primary" title="Take out of the blacklist">
            <i class="fas fa-share-square"></i>
            </button>
            <button class="btn btn-sm btn-danger" title="Delete user">
            <i class="far fa-trash-alt"></i>
            </button>
          </td>
        </tr>
      <% end %>
      </tbody>
  </table>
<% end %>
</div>

<div id="user_managment" class="tabcontent">
<table class="table table-striped table-bordered" id="user_table">
    <thead>
      <strong> User managment</strong>
    <tr>
        <th style="text-align:center">Id</th>
        <th style="text-align:center">Email</th>
        <th></th>
    </tr>
    </thead>
    <tbody id="user_table_body">
    <% @all_users.each do |user| %>
      <!-- Body -->
      <tr>
        <td><%= user.id%></td>
        <td><%= user.email%></td>
        <td>
          <button class="btn btn-sm btn-primary" title="Go to profile">
            <i class="fas fa-eye"></i>
          </button>
          <button class="btn btn-sm btn-primary" title="Edit">
            <i class="far fa-edit"></i>
          </button>
          <button class="btn btn-sm btn-warning" title="Send to blacklist">
            <i class="fas fa-share-square"></i>
          </button>
          <button class="btn btn-sm btn-danger" title="Delete user">
            <i class="far fa-trash-alt"></i>
          </button>
        </td>
      </tr>
    <% end %>

    </tbody>
</table>
</div>
</div>